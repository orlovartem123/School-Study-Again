@{
    ViewData["Title"] = "DeleteMedal";
}

<div class="text-center">
    <h2 class="display-4">Delete medal</h2>
</div>

﻿<form class="container-fluid" style="max-width: 580px;" method="post">
    <div class="form-group">
        <div class="row justify-content-between">
            <label class="col-lg-4">
                Medal
            </label>
            <div class="col-lg-8">
                @*<select id="car" name="medal" required class="form-control" asp-items="@(new SelectList(@ViewBag.Medals,"Id", "Name"))"></select>*@
                <select name="medal" required class="form-control">
                    <option>1</option>
                    <option>2</option>
                </select>
            </div>
        </div>
    </div>

    <div class="form-group row">
        <div class="col-lg-12">
            <button type="button" style="min-width: 100px" class="btn btn-sm btn-light">
                Delete
            </button>
        </div>
    </div>
</form>